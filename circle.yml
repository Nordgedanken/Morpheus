machine:
  environment:
    OS: linux
    DESKTOP: true
    GOROOT: /usr/local/go
    GOPATH: /home/ubuntu/.go_project
dependencies:
  pre:
  override:
      - if [[ "$OS" == "linux" ]]; then chmod +x ./.ci/linux.sh && ./.ci/linux.sh; fi
      - go get -u -v github.com/therecipe/qt/cmd/...
      - /home/ubuntu/.go_project/bin/qtsetup full desktop
      - go get -v github.com/Nordgedanken/Neo/...
  post:
test:
  pre:
  override:
    - if [[ "$DESKTOP" == "true" ]]; then /home/ubuntu/.go_project/bin/qtsetup full desktop && /home/ubuntu/.go_project/bin/qtdeploy test desktop; fi
  post:
